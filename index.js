Number
const n = 10;

String
const apos_string = 'Hello';
const quot_string = "Hello";
const tick_string = `Hello`;

console.log(apos_string === quot_string)

const kalimat = `${apos_string} ${n}`;
kalimat

const enter = `a

b`
enter

// Boolean
const b = true

y = apos_string + b
y

x = Boolean('b')
x

// Array
r = [n, n, quot_string, n, n, n];
r


rs = Array.from(apos_string);

console.log(apos_string.length)

console.log(r[0])

// Object = sesuatu yang kompleks
const o = {
    Nama: 'Rizal',
    Kelas: 'Web',
    Arr: r,
    Partner: {
        Nama: 'Rio',
        Kelas: 'Web',
    namadankelas: function(){
        return this.Nama + this.Kelas
    }
    }
}
console.log(o.Arr);
console.log(o.Nama);
console.log(o.Partner.Nama);

// Funcion
function tambah(a, b) {
    return a + b
}

console.log(tambah(100, 105522))

function kali(a, b) {
    return a * b
};

console.log(kali(100,233))
